/* globals describe, it, describe */
import { expect } from 'chai'

// const CopyWebpackPlugin = require('./../dist/index')

describe('CordovaConfigWebpackPlugin', () => {
  'use strict'
  it('Initial', () => {
    expect(false).to.equal(true)
  })
})
